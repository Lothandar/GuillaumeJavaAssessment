package firstAssessment;

public enum Intros {
	intro1,//find a way to write the intro other than replace all space by _ and replace them back to space when reading
	intro2,
	intro3,
	intro4,
	intro5,
}
